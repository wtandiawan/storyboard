<li>
  <span class="content">
  <h3><%= link_to story.title, story %></h3>
  </span>
  <% if @condition == "front" %> 
  <h4>by <%= link_to User.find(story.user_id).username, User.find(story.user_id) %></h4>
  <% end %>
</li>
